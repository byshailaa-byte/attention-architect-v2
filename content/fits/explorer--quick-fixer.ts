import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} follows the live thread of what's interesting, and for an Explorer the wander often is the work — every redirect snaps a thread that was actually going somewhere. Neither of you is wrong; it's a track you just can't see yet.`,
};
