import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs to sink fully into one thing to do {{child_pronoun_poss}} best work, and the slowing is depth, not stalling — your push speeds up the exact thing that needs to slow down. Neither of you is wrong; you're both chasing the same result, just pulling opposite directions on the clock.`,
};
