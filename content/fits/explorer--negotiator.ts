import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} is pulled by whatever's genuinely interesting right now, and a reward an hour away can't compete with that pull — the deal keeps losing to the present moment. Neither of you is wrong; your trade is reasonable, {{child_pronoun_poss}} attention just doesn't run on later.`,
};
