import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs real intensity to engage — low-key tasks barely register — and smoothing out the friction only makes the task flatter, since friction and challenge are part of what a Live Wire finds stimulating. Neither of you is wrong; making it easier is exactly the wrong direction for {{child_pronoun_poss}} attention.`,
};
