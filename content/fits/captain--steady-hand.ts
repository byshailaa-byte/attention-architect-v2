import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} wants to lead, and your calm frame without needing to dominate gives a Captain real room to command — a genuinely good match. The one tension: a Captain will test every line you hold firm just to check who's really in charge, which can read as defiance when it's really just role-checking.`,
};
