import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} is driven by building it {{child_pronoun_poss}} own way, and an Inventor isn't holding out for a better deal — the hold is for ownership of the method, which no incentive can buy. Neither of you is wrong; you're offering a fair trade for something that was never actually about the reward.`,
};
