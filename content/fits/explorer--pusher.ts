import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}}'s attention runs on curiosity, and pressure narrows an Explorer's focus onto the one thing least worth focusing on — the harder you push, the flatter the output gets. Neither of you is wrong; what's needed is a channel for the curiosity, not a cap on it.`,
};
