import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs it to stay {{child_pronoun_poss}} own choice, and every rescue quietly moves the decision back to you — a Storm switches off the instant ownership leaves {{child_pronoun_poss}} hands. Neither of you is wrong; you're removing the struggle, but you're also removing the ownership.`,
};
