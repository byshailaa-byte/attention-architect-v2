import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs to figure it out {{child_pronoun_poss}} own way — the wrong turns are how the learning happens — and every rescue quietly tells an Inventor {{child_pronoun_poss}} way wasn't working, which kills the drive to build. Neither of you is wrong; you're saving time, the message landing is that {{child_pronoun_poss}} method doesn't count.`,
};
