import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs it to be {{child_pronoun_poss}} own idea, and you lean in the moment {{child_name}} checks out — that loop is the homework battle. Neither of you is doing anything wrong, you're just not on the same side yet.`,
};
