import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} wants to run the show, and a Captain will negotiate — but only on {{child_pronoun_poss}} own terms, not accepting yours. Neither of you is wrong; you're both willing to bargain, you just both want to hold the gavel.`,
};
