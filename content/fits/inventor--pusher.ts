import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs it to feel like {{child_pronoun_poss}} own design, and the harder you push a set method, the more an Inventor digs in — not out of defiance, but because someone else's blueprint removes the only thing that makes {{child_pronoun_obj}} want to build at all. Neither of you is wrong; you're trying to give a shortcut, {{child_name}} is trying to keep the method {{child_pronoun_poss}} own.`,
};
