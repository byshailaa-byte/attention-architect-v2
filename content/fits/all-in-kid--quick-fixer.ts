import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs to go all the way down into one thing before it clicks, and your rescue tends to arrive right as the dive is starting — pulling {{child_pronoun_obj}} back out of the very friction that needed pushing through. Neither of you is wrong; you're helping, it just lands as an interruption.`,
};
