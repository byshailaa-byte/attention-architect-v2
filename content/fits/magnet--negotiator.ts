import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} is motivated by being in it together, and no reward can replace presence for a Magnet — {{child_pronoun_subj}}'d rather do it with you for nothing than alone for a prize. Neither of you is wrong; the currency that actually matters isn't a reward, it's you, there.`,
};
