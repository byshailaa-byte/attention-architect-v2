import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} can't settle until everyone feels okay — tracking the emotional room before focus can happen — and you solving the practical problem fast doesn't help while the question of whether you're annoyed is still open. Neither of you is wrong; the task was never the real blocker, the emotional read was.`,
};
