import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs it to be {{child_pronoun_poss}} own choice, not just a good deal — a trade is still your frame, and a Storm takes the reward but resents the arrangement. Neither of you is wrong; your trade is fair, it just isn't {{child_pronoun_poss}} idea.`,
};
