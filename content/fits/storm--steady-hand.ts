import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs ownership and immediacy, and your calm, steady approach actually serves a Storm well — you don't escalate. The one strain: even a structure held calmly is still structure {{child_pronoun_subj}} didn't choose, and a Storm will push on it.`,
};
