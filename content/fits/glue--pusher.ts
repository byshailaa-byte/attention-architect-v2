import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs the people around {{child_pronoun_obj}} to feel connected and okay before {{child_pronoun_poss}} focus frees up, and to a Glue, your push reads as the relationship is now strained — the exact thing that shuts {{child_pronoun_obj}} down. Neither of you is wrong; you're driving results, {{child_pronoun_subj}} can't focus while feeling the tension between you.`,
};
