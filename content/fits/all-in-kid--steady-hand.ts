import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} goes deep, and your calm, steady frame around {{child_pronoun_obj}} is close to ideal for an All-In Kid — you're genuinely 90% aligned. The one strain: a scheduled transition ("time to switch now") lands as being yanked out mid-dive.`,
};
