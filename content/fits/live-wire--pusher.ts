import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} runs on stimulation and intensity, and your push half-works because it is stimulating — but it's the stressful kind, and it burns the relationship as fuel to get there. Neither of you is wrong; charge is genuinely needed, just not the kind that comes from friction with you.`,
};
