import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs to be in charge of {{child_pronoun_poss}} own work, and every time you step in to fix it, a Captain gets quietly demoted in {{child_pronoun_poss}} own project — and a demoted Captain checks out entirely. Neither of you is wrong; there's a way to help that keeps {{child_pronoun_obj}} the one giving orders.`,
};
