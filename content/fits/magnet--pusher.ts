import type { FitContent } from "@/content/types";

export const fit: FitContent = {
  fitReveal: `{{child_name}} needs company in the work, and pushing {{child_pronoun_obj}} toward solo focus removes the one thing that switches {{child_pronoun_obj}} on — connection — while adding pressure on top of the loss. Neither of you is wrong; you're building independence, {{child_name}} is wired to work with, not alone.`,
};
