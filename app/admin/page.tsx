// Phase 7 will build the full admin dashboard.
// This stub exists to verify /admin auth from the first commit.
// honest_flag and honest_trigger MUST NOT appear in any admin view — Gate 3.

export default function AdminPage() {
  return (
    <main className="p-8" style={{ background: "var(--paper)", color: "var(--ink)" }}>
      <h1 className="text-2xl font-bold mb-2">Admin</h1>
      <p style={{ color: "var(--ink-dim)" }}>Dashboard coming in Phase 7.</p>
    </main>
  );
}
