// Week ordering for a user's program.
// Week 1 is always fixed (homework start / The Opening Choice).
// Weeks 2–6 prioritise the user's weakest axes so the program addresses
// the most-needed areas first.
//
// [STUB] Axis-to-content mapping will be completed once weeks 2–6 are authored.
// For now, returns a fixed sequence [1, 2, 3, 4, 5, 6].
export function getWeekOrder(_weakestTwo: string[]): number[] {
  return [1, 2, 3, 4, 5, 6];
}
